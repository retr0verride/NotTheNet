"""Infrastructure layer — concrete adapters.

All imports from third-party libraries, OS modules, and framework-specific
code belong here.  The domain and application layers never import from
``infrastructure/``.
"""
